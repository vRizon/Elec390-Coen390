<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Detectoma" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorControlActivated">@color/darkGreen</item> <!-- Green when checked -->
        <item name="colorControlNormal">@color/green</item> <!-- Green when unchecked -->

        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <style name="Theme.Detectoma" parent="Base.Theme.Detectoma" />
</resources>