<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Instructions TextView -->
    <TextView
        android:id="@+id/instructionsTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Follow the instructions below to take an accurate distance measurement."
        android:textSize="16sp"
        android:textAlignment="center"
        android:paddingBottom="20dp" />

    <!-- Start Distance Measurement Button -->
    <Button
        android:id="@+id/startDistanceMeasurementButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Distance Measurement"
        android:layout_gravity="center_horizontal"
        android:padding="16dp"
        android:minHeight="48dp"
        android:backgroundTint="@color/darkGreen"
        android:textColor="@android:color/white" />

</LinearLayout>
